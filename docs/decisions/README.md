# Architecture Decision Records (ADRs)

This directory is the **audit trail** for the project — the single most important artifact for
reviewing AI-built code (ENGINEERING_STANDARDS §4). Each ADR is a numbered, **immutable-once-accepted**
record of one non-trivial decision: *problem → options → decision → consequences*. It turns "why did
the agent build it this way?" into a reviewable trail.

Format: **[MADR](https://adr.github.io/madr/)**. Start from [`0000-template.md`](0000-template.md).

## Rules

- **One ADR per non-trivial decision.** `NNNN-kebab-title.md`.
- **Immutable once accepted.** To change a decision, write a **new** ADR that supersedes the old one
  (update the old one's `status:` to `superseded by ADR-XXXX`); never rewrite history.
- **Cite the source.** Every ADR links the relevant `DESIGN.md` / `DEVELOPMENT_PLAN.md` /
  `RESEARCH_SURVEY.md` / `ECOSYSTEM_AND_COUPLING.md` sections.
- **Number BLOCKS, not `ls`-then-guess (ADR 0029).** With parallel work lines (ADR 0028) two sessions that
  both pick "the next number" collide. Take the next free number **from your line's block** and add your row
  to **your line's subsection** below, so two lines never touch the same lines of this file:

  | Block | Owner |
  |---|---|
  | 0001–0029 | shared history + the cross-cutting/workflow decisions (integrator) |
  | **0030–0049** | **line S** — Component-S science |
  | **0050–0069** | **line M** — multi-cell coupled S+F+E |
  | **0070–0079** | **line E** — Component E vs observations |
  | **0080–0089** | **line O** — online coupling (Terrarium / SpeedyWeather) |
  | 0090+ | reserved (next line / integrator) |

## Index — the FROZEN Phase-0 decisions

| # | Decision | Status |
|---|---|---|
| [0001](0001-phased-hybrid.md) | Build a phased hybrid (emulate slow S, keep physical F, add energy E) | accepted |
| [0002](0002-emulate-distributions.md) | Emulate the trait/size **distribution**, not individual trees | accepted |
| [0003](0003-flux-then-integrate-carbon.md) | Flux-then-integrate carbon conservation (with fire + establishment) | accepted |
| [0004](0004-constant-co2-regime.md) | Constant-CO₂ regime (inherited from `with_nitrogen="no"`) | accepted |
| [0005](0005-drf-baseline-escalation.md) | DRF baseline for S + an escalation ladder | accepted |
| [0006](0006-reuse-terrarium-seb.md) | Reuse Terrarium.jl's SEB + skin temperature for component E | superseded by ADR 0017 |
| [0007](0007-julia-primary-stack.md) | Julia-primary stack (Python only for the S prototype) | accepted |
| [0008](0008-documentation-only.md) | Documentation-only (Documenter.jl); no AI code-wiki | accepted |
| [0009](0009-ssh-deploy-key-auth.md) | SSH deploy-key auth from the HPC | accepted |
| [0010](0010-s-prototype-biome-stratified.md) | S prototype = biome-stratified multi-cell (F/E single cell) | accepted |
| [0011](0011-reuse-global-ground-truth.md) | Reuse existing global (annual) ground truth; daily re-run is the gap | accepted |
| [0012](0012-canonical-slow-emulator-here.md) | Canonicalize component S here; port the sibling once, then abandon it | accepted |
| [0013](0013-main-only-workflow.md) | Work on `main` directly — no branches/PRs/branch-protection (relaxes §1) | **superseded by ADR 0028** |
| [0014](0014-differentiable-fast-core-first.md) | Fast core F is differentiable from the start (`F_diff`); C binary is oracle-only (supersedes F1/F2 split) | accepted |
| [0015](0015-reuse-map.md) | Reuse map for F_diff — what to TAKE/REDO/SKIP from the reference repos, + citations | accepted |
| [0016](0016-nn-hook-training.md) | Hybrid learned closures in F_diff: NN λ/Vcmax correction hooks, TBPTT online-rollout training, shipped as a package extension | accepted |
| [0017](0017-self-contained-energy-closure.md) | Implement component E self-contained (reimplement the SEB physics); no Terrarium runtime dep (supersedes 0006's implementation choice, keeps its physics) | accepted |
| [0018](0018-growth-ownership-split.md) | Growth-ownership split: F_diff owns representative-individual carbon growth; S owns the distribution + demography | accepted |
| [0019](0019-component-s-port-inference-not-call.md) | Component S: port inference to pure Julia (not call Python); wrap the machinery (not port the DirectEmulator wholesale) | accepted |
| [0020](0020-component-s-flux-driven.md) | Component S is **flux-driven** (flux-then-integrate), not climate-equilibrium — condition on F's delivered fluxes + AR state + slow bioclimatic boundary; drop this-year raw climate; climate-only DirectEmulator kept as the OOD benchmark (refines 0002/0003/0018, overrides 0019's "climate-only in P1" clause) | accepted |
| [0021](0021-component-s-native-julia.md) | Component S is trained + run in **native Julia** (EvoTrees.jl/DRF + Lux + Julia copula), dependency-light, no Python at runtime; Python confined to building the training table + running the DirectEmulator OOD benchmark; **build S once** (supersedes ADR 0019's "port Python inference" mechanism; learned S ships via a package extension per ADR 0014) | accepted |
| [0022](0022-component-s-handrolled-drf.md) | Component S's learned count/marginal model is a **hand-rolled zero-dep Julia DRF** (`src/drf.jl` + hand-rolled Xoshiro), not the EvoTrees package — keeps `[deps]`/`[weakdeps]` empty and CI free of dependency-churn risk (refines ADR 0021's "EvoTrees.jl **/DRF**" mechanism; EvoTrees verified as a fallback) | accepted |
| [0023](0023-component-s-production-drf-runtime-consistency.md) | Component S's **production DRF loads from a pure-Base serialized artifact** (`DRF.save_forest`/`load_forest`) and is **trained on a runtime-consistent feature table** (exact `flux_feature_vector` order); `age_mean` trained as the elapsed-year counter (not mean Age); `soilmoist`/`lai` are documented proxies pending the global C-`LAI_STAND`/`swc` pipeline; Gate-3 oracle is a drift alarm (implements P1 Tier-1 Step 4 on ADR 0020/0021/0022/0014) | accepted (`age_mean` §3 superseded by ADR 0024) |
| [0024](0024-component-s-dynamic-membership-and-true-age.md) | Component S has a **dynamic cohort roster** — establishment APPENDS a real age-0 recruit cohort, a K-cap MERGE bounds it, an atomic rebuild keeps every length-K `FDiffFastCore` field + `s.age` consistent (design risk #5) — and a **true nind-weighted mean cohort age** (DRF retrained on `mean(Age−1)`; `age0` seed carried in the meta); the Gaussian-copula recruit-trait sampler is wired into establishment (opt-in); the Gate-3 oracle compares on the C ≥5 m `ind`-output basis (**supersedes ADR 0023 §3's `age_mean` counter**; completes P1 on ADR 0018/0020) | accepted |
| [0025](0025-component-s-recruit-trait-copula.md) | Component S reproduces LPJmL-FIT's within-cell **trait distribution** via a trained, serialized **recruit-trait Gaussian copula** — 4 live primaries `{SLA, Wooddens, D95max, minwscal}` (`beta_2` is compile-time dead), trained on FIT's **survivor** marginals (mortality is trait-blind ⇒ community dist == establishment dist), flux-conditioned by `live_flux_cond`, sampled at establishment + derived-not-learned rest; pure-Base `.rcop` serialization; validated single-cell (plumbing) + multi-cell **K-fold-by-cell OOS** (skill); default OFF ⇒ baselines byte-identical (refines ADR 0018/0020/0022/0024) | accepted |
| [0026](0026-component-s-pooled-multiregime-transient-boundary.md) | Component S is **ONE pooled multi-regime emulator with a TRANSIENT (time-varying) bioclimatic boundary** — `gdd5`/`tas_cold_month` recomputed on a **trailing W-yr window** per (cell,year) (mirrors FIT's ~20-yr Climbuf; W→∞ recovers the static boundary), so the establishment gate tracks the warming; scenarios (+ later climate models) are **pooled** training data conditioned on climate+soil (one copula + one count DRF, not per-scenario); **CO₂ stays constant** (keeps ADR 0004); eval adds **hold-out-by-scenario**; opt-in per-year boundary series, default OFF ⇒ baselines byte-identical (refines ADR 0020/0023/0025, keeps 0004) | accepted |
| [0027](0027-adopt-transient-boundary-production.md) | **Adopt the TRANSIENT boundary as the production config** for the pooled multi-regime flux-driven emulator — records ADR 0026 §5's outcome: hold-out-by-scenario is a **wash** (transient vs static, counts R²=0.9847≈0.9844/0.9848, traits mixed KS≤0.05), and cost is negligible (offline ~1 min; online Climbuf ~0% slower), so **physical correctness breaks the tie** (a frozen establishment gate is wrong under +80 yr warming; matches FIT's Climbuf). The flux drivers (ADR 0020), not the boundary, carry the unseen-regime generalization. Code opt-in/default-off mechanism stays (guardrail 4); SLA/minwscal dip is a follow-on caveat (owner decision) | accepted |
| [0028](0028-parallel-session-lines.md) | **Parallel session lines: one long-lived branch + git worktree per line** — `line/{S,M,E,O}` in `wt-{S,M,E,O}`, line identity resolved from the worktree's branch by a `SessionStart` hook, CI on `line/**` (not `docs.yml`), **self-merge to `main` on green branch CI** (still no PRs, no branch protection, no review gate). Fixes the shared-checkout (`test/Manifest.toml` destruction) and shared-tip (non-fast-forward + unattributable CI) failures. **Supersedes ADR 0013** via its own "second contributor (human or otherwise)" reinstatement clause | accepted |
| [0029](0029-line-split-and-ownership.md) | **Four work lines (S / M / E / O) with per-path exclusive ownership, frozen cross-line contracts, and per-line coordination files** — S=Component-S science, M=multi-cell coupled S+F+E (owns the `run.jl`/`interface.jl` seam), E=Component E vs observations, O=online coupling; `lines/<X>/{STATE.md,JOURNAL.md}` + `changelog.d/` fragments replace same-file section edits (conflict-free by construction); `MEMORY.md` slimmed to shared facts + a router; ADR number blocks; the S→M artifact/feature contract frozen + versioned (companion to 0028) | accepted |

### Line ADRs — append to YOUR line's block (ADR 0029)

Per-line subsections keep two concurrent lines off the same lines of this file. Empty until a line files its
first ADR.

#### Line S — Component-S science (0030–0049)

| # | Decision | Status |
|---|---|---|
| [0030](0030-component-s-basis-clean-noise-floor.md) | **The trait gate is scored on the emulator's OWN stem population, against an attenuation-corrected ceiling** — the seed1-vs-seed2 floor is measured on the copula table itself (two `MODE=copula` builds differing only in `SEED`), gated by a `seed1-basis ≥ 0.99` cross-check against an independent parquet re-derivation (reads 1.000); `floor_r` is a realization-vs-realization r, so the reachable ceiling is `√(rel_P·rel_Y)` and the headline metric is `(GAP, r_center)` + the between-cell dispersion ratio. Exact per-axis headroom: Wooddens +0.226 > minwscal +0.153 > SLA +0.115 ≈ D95max +0.102 (D95max was NOT "at floor"); split-half proves the floor is trajectory divergence, not finite-stem noise. Withdraws the pre-S1 "floor 0.90–0.97" and "median instability" claims; S2 targets restated, and blocked behind 0031 | accepted |
| [0031](0031-component-s-complete-tree-pft-basis.md) | **Component S must train on FIT's COMPLETE tree PFT set (ids 0–6)** — `TREE_TYPES = [1,2,3,4,5]` (ported from a stale sibling yaml, never scoped by any ADR) drops the tropical broadleaved evergreen (id 0) and the boreal larch (id 6) = **32.5 % of survivor tree stems**, makes **16.7 % of tree-bearing cells invisible** (the tropical belt + Siberian larch), and biases the cells it keeps (per-cell trait medians correlate 0.09–0.97 between the two populations; the full set has 1.3–2.7× more between-cell spread). One imported constant, re-derive → retrain → re-validate → re-measure 0030, versioned artifacts, **integration point with M**; feature contract and runtime unchanged. Also records the `growth_eff` ÷`max(lai,EPS)` 1e9-outlier hazard where `LAI_STAND == 0` | accepted |
| [0032](0032-hainich-demo-drf-stale-feature-basis.md) | **The committed Hainich demo count DRF is trained on the RETIRED PROXY features while the `.rcop` beside it is on the REAL ones** — one emulator, two conditioning bases. `drf_forest_hainich.drf` (last written 2026-07-23, ADR 0024) carries `soilmoist = 0.7` / `lai = 21.2` / `growth_eff = 19`; `recruit_copula_hainich.rcop` (2026-07-27, ADR 0025) and a fresh build carry 0.85 / 3.07 / ~151 — the runtime magnitudes (`slow.jl:489/498`, `fast.jl:369`). A live ADR-0023 train/inference shift on 2 of 11 head features, masked by the DRF's OOD leaf-clamping. **Proven NOT caused by ADR 0031**: the control build (PRE vs POST) agrees to max|abs diff| = 0 on all 15 columns. Real basis is correct; regenerate `.drf` + `.rcop` TOGETHER as **milestone S1c** with re-measured drift thresholds — an integration point with M, never a side effect of the widening. Global artifacts unaffected | accepted |
| [0033](0033-truncation-was-the-composition-defect.md) | **The truncated tree-PFT basis WAS the missing-composition defect — widening it improved per-cell trait skill on EVERY axis, so S3 goes back to being a fallback.** Re-running the ADR-0030 gate on ids 0–6 (`seed1-basis` = 1.000 ×4, 52 165 cells) *falsifies* ADR 0031's prediction that a pooled single marginal would fit worse: Wooddens `emu_r` **0.567 → 0.807** (dispersion 0.546 → 0.718), minwscal GAP **+0.153 → +0.039 (near ceiling)**, SLA and D95max also up. Mechanism: the truncation removed the two PFTs whose composition is MOST predictable from environment (tropical belt, Siberian larch), leaving ids 1–5 in overlapping envelopes — so it selected the sub-population where conditioning has least to say, and that was misread as a model-class limit. **Reverses ADR 0031's promotion of S3; re-baselines the S2 gate** (S1b already closed 30 % of the Wooddens GAP with no conditioning change); supersedes ADR 0030's headroom table while keeping its method. Withdraws the "copula regresses to the global mean" claim | accepted |
| [0034](0034-runtime-training-feature-band-shift.md) | **Regenerating the demo `.drf` (S1c) closed the artifact-basis SPLIT but not the runtime↔training SHIFT — 4 of 15 feature columns are still outside the trained band, from three separate causes.** The two artifacts now agree (0 violations on the 8 shared conditioning columns) and every drift threshold improved (Gate-3 Height `nqrmse` 0.3895 → **0.2998**, median ratio 1.25 → 1.13, count ratio 0.67 → 1.28; alarm **tightened** 0.45 → 0.40), because in-domain `bm_inc_cell`/`growth_eff` raise the settled count 6.8 → 12.9 stems/patch. But `water_stress` (0.33 vs [0, 0.043] — an **F_diff-vs-C** difference, line M's), `soilmoist` (year-end instant vs the annual-mean training basis) and `lai`/`fpc` (single patch vs the C's cell-mean `LAI_STAND`) remain shifted. Records the *proof* that a target-band assertion can never detect this (a DRF prediction is a convex combination of leaf means), and ships the mechanism that can: `feature_history` + `feat_min`/`feat_max` in the meta + a CI assertion pinning the out-of-band set. Routes the causes: **S1d** (temporal/spatial basis, before S2) and an M integration point (`water_stress`); rejects retraining on runtime-produced features | accepted |
| [0035](0035-soilmoist-was-the-wrong-variable-and-lai-the-wrong-patch.md) | **`soilmoist` was not a time-aggregation mismatch — it was the wrong VARIABLE; and per-patch stand LAI IS reconstructable from `ind` after all.** Closes S1d, and *corrects* ADR 0034's diagnosis of both S-owned columns. The training `soilmoist` reduced the C `swc` output = total water over **SATURATION** capacity (`update_daily.c:411`) while the runtime fed `state.w` = plant-available water over **WHC** — different quantities, so no time re-reduction could ever agree, and `swc` is not invertible (no `wsats`/`wpwps` output). Both sides now use `ROOTMOIST / Σ_{l<3} whcs[l]`, the root-zone `whcs`-weighted mean of `w` at year end (new `build_rootmoist_soilmoist_feature.py` + `root_zone_soilmoist` in `slow.jl`) — which is what `FToS.soilmoist` always claimed to be. And `LAI` + `fpc_ind` between them carry the crown area, so the **per-patch** stand LAI is exactly `Σ LAI·fpc_ind/(1−exp(−k_pft·LAI))` — validated against the C's own height allometry at median rel err **1.8e-8**, replacing the `LAI_STAND` cell-mean join and fixing the `growth_eff` divisor with it. `fpc` needed no change (it was already per-patch both sides; ADR 0034 mis-grouped it). `soilmoist` is now **IN** band and `lai` fell 2.9× → 0.086×, so the pinned set is `{water_stress}` alone; DIRECT copula-draw bounds **tightened** 0.22 → 0.10 / 0.12 → 0.06. Rejects a runtime annual-mean accumulator (needs a daily hook in line M's `run.jl`, and the year-end STATE reading is the internally consistent one) | accepted |
| [0036](0036-biomass-and-size-are-validated-as-diagnostic-axes.md) | **Biomass and size are validated as APPENDED DIAGNOSTIC copula axes, on the `t8` global generation.** Two decisions: (1) `t8` re-derives every GLOBAL table + artifact on the ADR-0035 bases for historic, SSP370 and the pooled pair (`_t7` untouched; count OOS R² 0.9826/0.9823/0.9824, hold-out-by-scenario 0.982/0.9818, and the ADR-0030 gate PASSES with `seed1-basis` = 1.000 on all four axes over 52 165 cells); (2) the emulator's BIOMASS (`agb`) and SIZE (`Height`) distributions are measured by struct axes APPENDED to the copula table and **structurally excluded from the `.rcop`** — because `make_recruit_to_pools` maps exactly 4 axes onto carbon pools (ADR 0025, frozen, line M pins it) and because `agb`/`Height` are *outcomes* of the dynamics, so sampling them at recruitment would double-count F's allocation. Appending keeps every production prediction **bit-identical** (the per-axis RNG is seeded by axis INDEX), proven by a 50-cell `cmp` gate. Both struct axes come out **AT CEILING** on the ADR-0030 gate (`r_center` 0.987 / 0.986). Also records two defects this generation exposed: `polars` `collect(engine="streaming")` is non-deterministic in its emitted KEY SET at global scale (4 913 rows missing + 12 cells duplicated in one ssp370 build; the `drop_frac` guard cannot catch it because duplication makes the statistic negative) — fixed with a key-set invariant plus a window shift replacing the AR self-join, gated byte-identical; and the biomass `basis_ratio` was documented as a `Cov(N, mean size)` correction when it is an **exact identity** on matched rows, so it is really a row-universe consistency check | accepted |
| [0037](0037-the-trait-gap-is-an-estimator-defect.md) | **The per-cell trait GAP is an ESTIMATOR defect, not missing conditioning — milestone S2's premise is refuted as the primary cause.** Decomposing the ADR-0030 GAP with a direct per-cell regressor (K-fold by cell, harness self-validated against the documented numbers) attributes +0.080/+0.102/+0.089/+0.032 to the ESTIMATOR against +0.011/+0.025/+0.042/+0.004 to new covariates: Wooddens reaches r=0.916 and `sd_ratio` 0.896 from the EXISTING eight columns, already past the S2 targets of 0.889/0.75. Two estimator causes, each measured in isolation (ADR 0033). (a) CAPACITY: `SUBSAMPLE=50000` against ~158M rows gives **1063 leaves/tree for 54 020 cells**, ~51 cells sharing one conditional; raising it closes ~⅓ of the GAP but trades against the pooled marginal (`b6x2M` `sd_ratio` 0.678→0.770 while pooled `nqrmse` 0.013→0.023), and resolution beats averaging (`b12x500k` is worse than `b6x2M` with 2× the trees). (b) **`DRF.predict_quantile` was not implementing the quantile-regression-forest estimator** — it concatenated all leaf values unweighted, so a tree contributed in proportion to its leaf SIZE instead of `1/T`; verified non-inert first (leaf-size CV **2.01**, max 4016, one leaf taking **17-21 %** of every prediction vs QRF's **1.7 %**), and the bias is directional because a big leaf approximates the GLOBAL marginal, which is also why more trees never helped dispersion. Confound separated: the quantile-CONVENTION change accounts for 0.002-0.014 % vs the weighting's 1.67-4.43 %. Records that pooled-marginal metrics (`nqrmse`, pooled KS) are **structurally blind** to an under-resolved conditional — the same shape as ADR 0032. `DRF.predict` was already correct, so the count DRF is UNAFFECTED. Fixed opt-in and carried eval→train→`.rcop` meta→`RecruitCopula` so an artifact cannot be scored and served under different estimators (ADR 0023). (c) Extended conditioning ships as an additive **six-column** moisture/aridity tail (64-72 % of the full-28 gain; all 28 would push `mtry` 3-of-8→6-of-36 and `Xc` to ~57 GB) via a policy FACTORY, so no struct/format change and the count DRF's shared boundary tail is untouched. **S2's gate is NOT met and production enablement is DEFERRED** to a follow-up ADR until the QRF × capacity matrix completes | accepted |
| [0038](0038-conditioning-was-necessary-and-is-a-spatial-address.md) | **Conditioning was necessary after all — and the six columns that cross the gate are a spatial address.** The 12-rung QRF x capacity matrix completes and SUPERSEDES ADR 0037's thesis: the ESTIMATOR lever is larger on `emu_r` (+0.050 vs +0.037) but **saturates at Wooddens 0.867** (fitted asymptote 0.870; reaching 0.889 by capacity alone needs ~1000x the whole 197.7M-row table), so it cannot close the gap at any artifact size, while `ncond` 8->14 at FIXED 6x2M/d22/QRF=1 delivers +0.037 `emu_r` and **+0.0966 `sd_ratio`** — the larger lever on criterion 2, the axis that was failing — and carries both criteria across. **S2's premise is vindicated, not refuted**; ADR 0037's "second-order, ~4x smaller" is wrong by ~4x in the opposite direction, and its 0.916 was a LightGBM bound that did not transfer to the DRF. The ADR-0030 §4 criteria are **MET for the first time** by `env-qrf-b6x2M` (0.901 / 58.0 % of GAP / `sd_ratio` 0.8541 / pooled KS improving on all four axes / `r_center` gaining on all four), robust across five %GAP conventions (min 56.7 %) on a PAIRED comparison whose basis is identical at the INODE level. Ships as `recruit_copula_global_historic_t9.rcop` (508 MB, load 6.77 s = 71.6 MiB/s measured, byte-reproducible); `b6x8M` rejected (+0.002 for 4x bytes and worse KS on all four). **NOT promoted to line M's pinned production copula:** the six env columns have within-cell sd **exactly 0 for 100 % of cells**, so they cannot encode a warming response and a cell's historic and ssp370 rows are bit-identical on them; 1-NN on those columns reaches r=0.800 with the nearest neighbour **1.00°** away, and by-cell CV cannot separate a transferable response from spatial interpolation. Pins criterion 3 = the numeric pooled-KS bound (pooled `nqrmse` 0.0208 would fail the pooled BASELINE), and records the coherent -0.4 % SLA quantile bias KS hides. Closes three silent-failure paths: `.rcop` **format v2** carries `qrf` (v1 still loads => guardrail 4), the emulator rejects a conditioning-width mismatch at CONSTRUCTION rather than at first recruit, and the gate refuses a floor from two seeds that are the same seed — the **ssp370 `random_seed2` ground truth is a BIT-IDENTICAL COPY of seed1** because its config restarts from the historic seed1 `restart_2019.lpj` | accepted |
| [0040](0040-the-address-null-was-measured-on-the-wrong-basis.md) | **The address null was measured on the wrong basis — and the gate metric is nearly blind to warming.** REJECTS ADR 0038's proposed decision rule for the address-vs-response question: *"decays toward the 1-NN level (r~0.80) => it is an address"* uses a **hash-fold** address baseline as the null for a **blocked** evaluation, which is guardrail 7's reference-basis error. Measured fold-mode-matched null (1-NN, pooled 57 719 cells, fold designs read from the Julia code the forests run): Wooddens `geo` **0.837 under hash -> 0.140/0.210 under block(15°,5°)** at two colourings, so the rule was wrong by ~0.63 in r and a blocked 0.78 would have been declared "an address" when it is the opposite. PRE-REGISTERS the corrected rule before any forest log is read, and the surrogate screen already predicts the answer: the env tuple RETAINS 73 % of its hash skill under blocking (0.811->0.594) where a pure address retains 21 %, and the conditioning DELTA is ~invariant (+0.078 -> +0.076/+0.058). Adds a SECOND gate, because `emu_r` is a **level** statistic and `sd(Δobs)/sd(level)` is only **0.20-0.31** — it is 3-5x more sensitive to spatial interpolation than to the warming response production depends on. Measured from existing predictions: the shipped artifact **damps the mean Wooddens warming shift by 37 %** (`Rb` -892 [-1022,-756] vs +2433 observed) while capturing only 24-62 % of the transient PATTERN against a 0.87-0.96 split-half ceiling; the comparison arm is 4-lever confounded so causation awaits the matched `p8` rung. Ships the blocked-CV machinery opt-in and **verified** byte-identical (6/6 `pred_*.f64` `cmp`-identical; hash fold sizes reproduced), with `BUFFER_DEG=0` demoted to a sensitivity rung (a blocked split alone leaves **24.2 %** of test cells within 1° of training data via the block perimeter), the buffer verified by an INDEPENDENT great-circle metric, `mtry` exposed as the hidden fourth lever (sqrt(p) is 3 at ncond 8, 4 at ncond 14), and a rank-degenerate `p14geo` basis caught and fixed (`abs_lat` vs `cos_lat` Spearman **-1.000000**). Flags ADR 0038's saturation fit and "1052x the table" extrapolation as **unresolved**: they rest on +0.002/+0.003 increments against a spatial-sampling sd of order 0.01, with no seed replication | accepted |
| [0041](0041-ssp370-second-seed-needs-the-second-spinup.md) | **`random_seed` is inert under `FROM_RESTART`: a second seed is a second SPIN-UP.** Why every attempt at an ssp370 second-seed member produced a byte-identical clone of seed1, and how the real one was produced. With `"new_seed": false` the per-cell RAND48 seeds are restored from the restart file (`newgrid.c:507-513` → `freadcell.c:37`) and the `setseed` that would apply `config->seed_start` is **gated off** (`newgrid.c:520-521`); `seed_start` is applied once at parse time (`fscanconfig.c:231`) then unconditionally overwritten from the restart header (`openrestart.c:139-140`). The historic pair is independent only because its 1000-yr spin-ups ran *without* `-DFROM_RESTART`, taking `newgrid.c:460`'s **ungated** `setseed(…seed_start+(i+startgrid)*36363)` — arithmetically visible in the restart bytes (cell 156 `(13070,36533,86)` vs `(13070,36534,86)`). **And the log never says `Random seed: N`** (`fprintconfig.c:748-751` prints `Reading random seeds from restart file.`), which is why the clone survived: a floor built from it reports `floor_r ≡ 1` = fabricated headroom, with no error. Fix = repoint `restart_filename` at the historic seed-N restart, keeping `new_seed:false` (flipping it would discard 1020 yr of evolved RNG state at the scenario boundary — a discontinuity seed1 lacks). Records the member's provenance as **four** edits including a forced `co2` repoint (the seed1 path rotted when its directory was repurposed, making the committed seed1 config unrunnable; recovered file md5 `ed5699b9c92d4d25857889f644b153db` proven identical four independent ways). Also: **a subset re-run is NOT a per-cell replica of the global run** — same binary/restart/forcing, 1 cell diverges from the 2048-task truth at the first step, a 21-cell block is bit-identical 15 years then diverges (RNG ruled out as the cause; mechanism unestablished) ⇒ CLAUDE.md §3's decomposition-independence holds for the *seek*, not the *evolution*, a seed pair is valid only at **the same binary AND the same `--ntasks`**, and a cross-build equivalence gate must be matched-decomposition full-grid. Never judge a C run from SLURM state (the stock jcf always exits 0). | accepted |
| [0042](0042-the-env-tail-transfers-but-its-transient-benefit-was-interpolation.md) | **The env tail transfers across a severed neighbourhood — but its transient benefit was interpolation.** Adjudicates ADR 0040's PRE-REGISTERED rule on the completed 7-arm forest matrix, applying it verbatim: **RESPONSE — the six env columns are not merely a spatial address** `[PROVISIONAL pending one colouring, whose flip thresholds are fixed in §4 BEFORE the deciding rung landed]`. Wooddens `Delta_blocked` = **+0.0315** [+0.0011,+0.0633], `P(<=0)=0.021`, clearing the bar `0.5*Delta_hash` = +0.0201; the pure-position control collapses **0.1868 below the treatment and 0.1553 below no tail at all** (`P(fail)=0.0000`, all five folds), a sign flip (+0.0538 hash -> -0.1553 blocked) that no weak encoding can produce since the identical basis is the STRONGEST of seven arms under hash folds. The frozen surrogate screen predicted ~86 % retention and the forests delivered 78/119/137 % — screen and forests agree, having been frozen in that order. **REFUTES ADR 0040 §4's attribution**: the matched `p8` arm it asked for shows the ncond-8 baseline damps the Wooddens warming shift **39.9 % on its own** (`Rb` -971.5 vs the tail's -892.0) and the tail moves `Rb` TOWARD zero (+79.5 hash, +163.2 blocked), so §5 bullet 4's gate is simultaneously **met and mis-specified** — it is passable by a change that degrades the transient — and is re-specified on `Rb` AND `Rr` AND `|Ra-1|` at BOTH fold modes. Under that gate the tail FAILS: **`Rr` flips sign with fold design** (+0.0395 hash -> **-0.0305** blocked), so the two gates DISSOCIATE — the level delta survives blocking, the transient-pattern delta does not. `p14perm` proves `Rb`-nearer-zero is not better (least damping of any arm, worst pattern, zero information). Also: the published hash-fold gain is **reproducible by a pure address** (`p14geo-hash` 0.9231 > `p14env-hash` 0.9095 on all four axes), so ADR 0038's doubt was well founded and the defensible figure is the blocked +0.0315; **width COSTS skill at matched mtry** (`p14perm` -0.0201, z~9); the measured spatial sd of a Delta is **0.004-0.006 hash / 0.012-0.016 blocked**, superseding §7's single "order 0.01"; `Delta_blocked` is **one-fold-dominated** (fold 0 = 84 % of it, from the fold with the fewest training cells). Line M's re-pin refusal STANDS but is **re-grounded** — not "it might be an address" (settled: it is not) and not "it damps the transient" (measured: it does not), but that the tail is a frozen present-day hydrological envelope buying +0.031 of level skill while costing 0.031 of transient pattern. Ships `cell_env.parquet` (67 420 cells, gated on **exact float64 equality** against the shipped `Xc` tail over 200 000 real rows), clearing the mechanical half of M's blocker. Fixes three defects that each manufactured a plausible number: an inert `BLOCK_SALT` that would have fabricated a perfectly-agreeing replicate and FORCED a false RESOLVED (proven fixed: the salt-1 fold sizes genuinely differ), a **Float32** `group_by().mean()` accumulation putting a runtime-provisioned tail 3.35e-07 off the trained values (199 093/200 000 rows), and a bootstrap whose cluster labels were not row-aligned with the statistic (detected because a FIXED seed gave two different CIs). Corrects ADR 0040 §3(b)/§6.4 (`eco_diag_gdd_5`/`tas_cold_month` ARE transient on `pooled_w20`) and forbids quoting the retention ratio (0.783 +- 0.411) or blocked `sd_ratio` against criterion 2 (cross-basis; `P(pass)=0.416`). **0039 is permanently vacant.** | accepted |
| [0043](0043-crossbuild-gate-passes-builds-are-poolable.md) | **The cross-build gate PASSES: the Feb-5 and Jul-21 builds are bit-identical and poolable.** Closes the open question ADR 0041 specified but could not run to a verdict. The matched-decomposition gate (job `1678607`, full-grid 67 420 cells / **2048 tasks**, `-DFROM_RESTART` from the historic seed1 restart with `random_seed 1` — a faithful re-run of the seed1 member, only `write_restart` dropped) reproduces the seed1 ground truth **bit-for-bit**: `globalflux_2020_2100.csv` `cmp`-identical, `vegc` data identical across **all seven** variables by SHA-256 including the full 81×280×720 `VegC` field, and — beyond what the gate asked for — the **193 GB per-individual `ind` roster is `cmp`-identical over all 81 years**. That last result is the load-bearing one: `ind` is the finest grain the model emits and precisely the quantity ADR 0041 showed amplifies any perturbation into a permanently different row count, so the builds do not merely agree in the global mean, **they produce the same individuals**. ADR 0041's source-level arguments (the grass-GPP patch writes only into `initoutput.c`'s trash region; no physics parameter drifted) are now empirical rather than plausible. ⇒ The ssp370/pooled bases can carry a real ADR-0030 noise floor; the corrected seed2 member is both a genuine second realization *and* comparable to seed1. Also: **a file-level `cmp` on a NetCDF output is the WRONG test** — `vegc` differs by 124 B at byte 172 purely in the `history` attribute (a wall-clock timestamp + config path the model is required to write), so compare decoded variables or the timestamp-free text outputs. Records the **stale-job-id trap** that made this member's chained gate fail in 1 s (`no completion line at all`) on a run that had finished cleanly: a resubmitted run writes a NEW `lpjml_<jobid>.out`, so a jcf pinning the old id reads the cancelled attempt's 0-byte corpse — `diagnose_ind_seed_independence.py` now takes `--log-dir` and resolves the newest non-empty log, and an empty log is a distinct FATAL rather than a gate failure. The `STEM_CAP=400`-vs-uncapped mismatch is now the **only** remaining blocker on ADR 0030 criteria 1 and 4. | accepted |

#### Line M — multi-cell coupled S+F+E (0050–0069)

| # | Decision | Status |
|---|---|---|
| [0050](0050-per-cell-input-provisioning.md) | Per-cell input provisioning: `whc_nat` time-mean soil column + community-mean `getrootdist` root profile, gated on reproducing the committed Hainich column | accepted |
| [0051](0051-c-faithful-leafon-water-scalar.md) | F_diff's daily `wscal` was the realized supply/demand ratio; the C's is a POTENTIAL leaf-on index — opt-in `WaterParams.wscal_leafon` closes the last runtime↔training conditioning shift | accepted |
| [0052](0052-fdiff-missing-soil-ice.md) | F_diff has no soil ice: the CONFIRMED cause of the boreal water-stress residual (the C's root-zone `w` is 0.000 Nov–Apr, F_diff's 0.75) — plus a second, distinct dry-cell root-zone water bias | accepted |
| [0053](0053-fdiff-biome-oracle-basis.md) | The M3 F-side oracle: the C's daily fluxes are ALL-PFT (grass is up to 42 % of GPP) and the driver's modal patch is 1.12–1.72× denser than the C's 25-patch ensemble — both removed, which FLIPS the Sahel GPP verdict 1.03 → 0.75; year-matched ratios then separate flux bias from canopy drift | accepted |

#### Line E — Component E vs observations (0070–0079)

| # | Decision | Status |
|---|---|---|
| [0073](0073-nocturnal-h-is-a-ground-heat-timescale-error.md) | **Component E's nocturnal-H failure is a ground-heat TIMESCALE error, not an aerodynamic one (milestone E6).** Because `H` is the *exact* residual `Rn_m − LE − G_m`, its error obeys identically `ΔH = ΔRn − ΔG + ε_obs` (`ε_obs` = the tower's own non-closure) — **`g_a` is in none of those terms.** Measured: the closure's nocturnal `g_a` is within **0.7 %** of DE-Hai's measured-`u*` value, substituting the measurement makes night H **worse at all 4 sites**, and a **100× `g_a` bracket cannot reach positive nocturnal R²** ⇒ ADR 0072 item 6's stability-form hypothesis is **REFUTED** (its monotone sweep was bias cancellation). The mechanism is `G = λ_g(T_skin − t_soil)`: sd(`G_m`) is **5–7×** sd(`G_o`) at the forest sites, and 88 % of DE-Hai's night H bias is `ΔG`. Three independent lines (implied `λ_g` per site, sd-matching, daily H skill) give **`λ_g ≈ 1.0`, not 7.0**, at the daily step `run.jl:93` actually runs — daily H R² 0.03 → **0.64** (DE-Hai), 0.33 → **0.74** (AU-ASM). Also: `ε_obs` is **−62 / −47 W/m²** at AU-Tum / AU-Rob ⇒ those towers **cannot** score a closing model's nocturnal H. **No default changed** — `lambda_g` is the live E→M integration point | accepted |
| [0072](0072-e4-p2-observational-verdict.md) | **The P2 verdict (milestone E4, Experiment A — the closure alone, tower-forced with the tower's own LE, 534 488 steps at 4 sites):** `Rn` **VERIFIED** (R² 0.987–0.996), `T_skin` **VERIFIED** where observable (daily RMSE 1.4–2.0 K, R² 0.76–0.95 — not at Hainich, no `LWup`), `H` **verified in the MEAN but not in variability** (bias +11.4…−19.2 W/m², 76.3 % of DE-Hai daily means inside PLUMBER2's own band, but daily R² 0.087–0.778 and **nocturnal R² −1.0…−5.6**). Names the failure mode (**1–2 K night over-cooling**), shows the half-hourly R² is inflated by the diurnal cycle, and finds the stability correction right but its `stab_amp = 0.75` **too weak** (sweep monotone to 0.9) — **no default changed**; retune is an integration point with M. Frozen as a CI regression gate with two committed fixtures | **items 4+6 superseded by ADR 0073** (the rest stands) |
| [0071](0071-wind-psurf-from-obsclim.md) | **Component E's wind + surface pressure come from ISIMIP3a obsclim GSWP3-W5E5** (the same family as the run's own tas/pr/rsds/lwnet/huss), remapped onto orderA cells via `grid.nc cellid → (lat,lon) → source axis` with 29 Feb dropped for the noleap-365 model calendar. Gate PASSES at all 5 biome cells: `tas` round-trip vs `temperature_test.clm` `max|Δ| = 0.000 °C`, exact agreement with an independent label lookup, calendar verified against the LPJmL-prepared noleap wind, and Hainich vs the DE-Hai tower −10.1 % wind / +1649 Pa (≈143 m cell-mean elevation offset). Committed fixtures `wind_psurf_<biome>.csv`; **SSP370 psurf stays unsourced** (no `ps` in the raw GCM set); feeding the coupled driver is an **integration point with M** | accepted |
| [0070](0070-plumber2-observational-reference.md) | **PLUMBER2 v1-0 is Component E's observational reference** — 9 sites staged script-reproducibly from the anonymously-readable NCI THREDDS `ks32` collection (DE-Hai = the prototype cell + one tower per biome slot of `biome_coupled_tests.jl`); the dataset's own `*_cor_uc` supplies the E4 error band and `Ustar` an independent `g_a` check. Records the finding that **PLUMBER2's FLUXNET2015/LaThuile files carry no `LWup`** ⇒ **T_skin is not observable at Hainich** and is validated at the 4 OzFlux sites only (biome-analogously; no boreal site; AU-How suspect) | accepted |

#### Line O — online coupling: Terrarium / SpeedyWeather (0080–0089)

| # | Decision | Status |
|---|---|---|
| [0080](0080-licensing-basis.md) | **Licensing basis (P5): AGPL-3.0-or-later outbound; consume EUPL/MIT works as *library dependencies* (`[weakdeps]` + extension), never vendored** — AGPL is *forced*, being both what LPJmL-FIT's copyleft requires and an EUPL-1.2 Appendix Compatible Licence (Art. 5), so it is valid whether or not a dependency counts as a derivative work; Terrarium's `NOTICE` extends Art. 5 to **any** licence for normal library use ⇒ **P4 unblocked**; the READ/DEPEND/VENDOR tiers are separated (vendoring needs its own ADR); NeuralCrop.jl stays method-only (CC-BY-NC ↔ AGPL §7 undistributable) and LPJ_resilience reimplement-from-paper (unlicensed); the "port of NeuralCrop" provenance claim was checked by direct comparison, found to share no expression, and reworded; register + gate = `docs/third_party_licensing.md`. **Annotates (does not supersede) ADR 0017** — its driver 1 is retained for VENDOR only; corrects one ADR 0015 claim (obligations attach on *distribution*, and this repo is public). Residual: the owner files `LICENSE` (§4) | accepted |
| [0081](0081-owner-closes-licensing-reuse-authorized.md) | **Owner CLOSES the licensing question — reuse of LPJmL-FIT / Terrarium / SpeedyWeather is authorized (the owner is a member of BOTH the LPJmL-FIT group and TUM-PIK-ESM, which hosts them); the ONE standing obligation is transparent citation** across four surfaces (`docs/third_party_licensing.md`, `CITATION.cff`, `docs/src/refs.bib`, source headers). Do not raise licensing again; `LICENSE` is no longer a blocker or a TODO. NOT reopened: NeuralCrop.jl stays method-only (CC-BY-NC, different author, outside both groups) and runtime `[deps]` stays empty (ADR 0014 — a *technical* offline-compute-node constraint, so weakdeps+extension survives). Supersedes ADR 0080 §4 only | accepted |
| [0082](0082-online-esm-first-ownership-and-validation.md) | **The ONLINE configuration is ESM-FIRST and validated against OBSERVATIONS, not LPJmL-FIT** (owner decision) — **Terrarium owns skin temperature + SEB + soil water/thermal**; our contribution online is **vegetation** (S, FIT photosynthesis behind `AbstractPhotosynthesis`, FIT water-limited ET behind the pluggable `AbstractEvapotranspiration`, so LE stays ours but is solved *implicitly consistent with T_skin* — which Component E provably is not, `surface_energy_balance.jl:128/149`). Two explicit configurations: OFFLINE keeps guardrail 3 (C-binary oracle) unchanged, ONLINE scores against PLUMBER2/FLUXNET + observed vegetation; conservation binds both. **ClimBuf** cold-starts from a SpeedyWeather-only spin-up on its OWN climate (obsclim seeding rejected — SpeedyWeather's climate ≠ Earth's). **S's `soilmoist` must map to the layer-mean of Terrarium's `plant_available_water`, NOT `saturation_water_ice`** (porosity- vs WHC-normalized = a definitional mismatch) — diagnosis + possible retrain is an integration point with line S. Narrows ADR 0017's scope to offline without superseding it | accepted |
